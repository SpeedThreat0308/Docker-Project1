services:
  web:
    image: "web-app"
    build: .
    ports:
      - "8000:5000"
    depends_on:
      - redis
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
  redis: 
    image: "redis"
    volumes:
      - redis-data:/data
volumes:
  redis-data: